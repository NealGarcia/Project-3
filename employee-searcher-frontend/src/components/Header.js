import React from 'react';
import { Link } from 'react-router-dom';


function Header(props) {
    return (
        <div className="component">
            EMPLOYEE FINDER (this is the header)
        </div>
    );
}

export default Header;